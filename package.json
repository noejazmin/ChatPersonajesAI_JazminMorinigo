{
    "name": "chat-personajes-ai",
    "version": "1.0.0",
    "description": "SPA responsive para chatear con personajes ficticios usando Gemini AI y Vercel Functions.",
    "type": "module",
    "scripts": {
        "local": "npx --yes vercel dev",
        "test": "vitest run"
    },
    "dependencies": {
        "@google/generative-ai": "^0.21.0"
    },
    "devDependencies": {
        "vitest": "^4.1.8"
    }
}
